    cv2.VideoCapture(filename): 打开视频文件或摄像头设备。参数可以是视频文件路径或设备索引。示例：

    video.get(propId): 获取视频属性。propId是属性标识符，例如帧率、总帧数等。示例：

    video.set(propId, value): 设置视频属性。propId是属性标识符，value是要设置的值。示例：

    video.read(): 读取下一帧。返回一个布尔值和帧内容。布尔值表示是否成功读取，帧内容是图像数据。示例：

    cv2.imwrite(filename, image): 将图像写入文件。filename是文件名，image是图像数据。示例：

    video.release(): 释放视频对象，关闭视频文件或释放摄像头资源。

2. os (操作系统接口)

os模块提供了一些与操作系统交互的功能，包括文件和目录操作。

常用函数和方法：

    os.path.exists(path): 检查指定路径是否存在。返回布尔值。示例：

    os.makedirs(path): 递归创建目录。如果中间目录不存在，也会一并创建。示例：

    os.listdir(path): 列出指定目录下的所有文件和子目录。返回一个列表。示例：

    os.path.join(path, *paths): 拼接多个路径组件，生成一个完整的路径。示例：

os.path.splitext(path): 分离文件名和扩展名。返回一个元组